version: v1
name: buf.build/schemahub/schemahub
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
    - RPC_PASCAL_CASE
    - RPC_REQUEST_RESPONSE_UNIQUE
  ignore:
    - google/protobuf
breaking:
  use:
    - FILE