# SchemaHub frontend environment
# Copy to .env.local (gitignored) and adjust values.

# URL of the Envoy gRPC-Web bridge (backend gRPC is NOT reachable directly)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Public origin of this frontend (used for absolute links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
